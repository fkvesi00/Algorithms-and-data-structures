Za dvije sortirane liste L1 i L2 (mogu se pročitati iz datoteke ili unijeti ručno, bitno je samo da su 
sortirane), napisati program koji stvara novu vezanu listu tako da računa: 
a) L1∪L2, 
b) L1∩ L2. 
Liste osim pokazivača na slijedeću strukturu imaju i jedan cjelobrojni element, po kojem su 
sortirane.

For two sorted list L1 and L2 (they can be read from file or inserted manualy, its important they are sorted)
Write program wich make new linked list that does:
a) L1∪L2, (union)
b) L1∩ L2.(intersection)

List except pointer on next list have one int, they are sorted by that int